import {
  colorDarkBackground,
  colorDarkGunMetal,
  colorTertiary,
  colorWhite
} from "./GlobalVariables";

export const lightTheme = {
  background: `${colorTertiary}`, // antiquewhite
  font: `${colorDarkBackground}`, // antiquewhite
  primary: `${colorWhite}`
};
export const darkTheme = {
  background: `${colorDarkBackground}`, // black
  font: `${colorWhite}`,
  primary: `${colorDarkGunMetal}`
};
